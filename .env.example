# ── TDX 運輸資料 API ──────────────────────────────────────────────────────────
# 在 TDX 會員中心(https://tdx.transportdata.tw) -> 金鑰管理 建立應用程式後取得。
# 本機測試時複製這個檔案為 .env，填入實際值（.env 已加入 .gitignore，不會被 commit）。
# 部署到 Vercel 時，在專案的 Settings -> Environment Variables 設定同名變數。
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=

# ── Redis（Smart Nav Traffic Engine）─────────────────────────────────────────
# 選一種方式設定，兩者 API 相容：
#
# 方式 A：Vercel KV（推薦，最簡單）
# Vercel Dashboard → Storage → Create KV Store → 環境變數自動加入
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
#
# 方式 B：Upstash 直連（自行在 console.upstash.com 建立）
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
