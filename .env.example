# ── TDX 運輸資料 API ──────────────────────────────────────────────────────────
# 在 TDX 會員中心(https://tdx.transportdata.tw) -> 金鑰管理 建立應用程式後取得。
# 本機測試時複製這個檔案為 .env，填入實際值（.env 已加入 .gitignore，不會被 commit）。
# 部署到 Vercel 時，在專案的 Settings -> Environment Variables 設定同名變數。
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=

# ── Upstash Redis（Smart Nav Traffic Engine）────────────────────────────────
# 在 https://console.upstash.com 建立 Redis 資料庫後取得 REST URL 和 Token。
# 用於：在線人數 Heartbeat / Presence TTL / GPS rolling window / Traffic aggregation。
# 免費方案足以支援初期使用量。
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
